import { z } from "zod";

// Dead channel schemas (BlueBubbles, MSTeams) removed.
// Surviving channels: Web (built-in), NoxSoft Chat, NoxSoft Email.
// Extension channels can still use passthrough schemas in ChannelsSchema.

export const _emptyProvidersCoreMarker = true;
