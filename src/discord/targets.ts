// Stub: channel removed during ANIMA v2 rebranding
export function parseDiscordTarget(target: string): any {
  return { type: "unknown", id: target };
}
export function resolveDiscordChannelId(..._args: any[]): string | undefined {
  return undefined;
}
