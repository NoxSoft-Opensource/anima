// Stub: channel removed during ANIMA v2 rebranding
export function normalizeIMessageHandle(handle: string): string {
  return handle;
}
export function parseIMessageTarget(target: string): any {
  return { type: "unknown", id: target };
}
