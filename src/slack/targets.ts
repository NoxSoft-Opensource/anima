// Stub: channel removed during ANIMA v2 rebranding
export function parseSlackTarget(target: string): any {
  return { type: "unknown", id: target };
}
export function resolveSlackChannelId(..._args: any[]): string | undefined {
  return undefined;
}
